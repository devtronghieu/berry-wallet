export enum Event {
  Connect = "connect",
  Disconnect = "disconnect",
  SignIn = "sign-in",
  SignMessage = "sign-message",
  SignTransaction = "sign-transaction",
}

export enum Channel {
  Background = "background",
  Content = "content",
  Popup = "popup",
}
