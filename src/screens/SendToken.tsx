import CloseSquareIcon from "@/icons/CloseSquare";

const SendToken = () => {
  return (
    <div>
      <div className="flex">
        <span className="font-semibold text-secondary-500">SendToken</span>
        <CloseSquareIcon size={24} />
      </div>
    </div>
  );
};

export default SendToken;
